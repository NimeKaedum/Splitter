<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Renamed to Theme.LiveSplitLike to avoid confusion -->
    <!-- Parent is explicitly Material3...NoActionBar -->
    <style name="Theme.LiveSplitLike" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Status bar configuration -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- Navigation bar configuration -->
        <item name="android:navigationBarColor">@android:color/black</item>

        <!-- Crucial lines to ensure ActionBar is gone in edge cases -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
